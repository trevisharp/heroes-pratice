# heroes-pratice

# Run App

From the root, to run ui use npm run on a console:
```powershell
cd ui
npm run dev
```

From the root, in another console run docker to start backend and database:
```powershell
docker compose up --build
```

# Read API documentation

To view the API docs open the [swagger](http://localhost:3000/docs/).